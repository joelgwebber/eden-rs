    // let frame: HashMap<String, Node> = hash_map_from!(
    //     "a": expect,
    //     "b": expr,
    // );
    // let b = native_eq(Node::Dict(NodeRef::new(frame)));
    // match b {
    //     Node::Bool(false) => assert!(false),
    //     Node::Bool(true) => (),
    //     _ => panic!("invalid argument to assert: {}", b)
    // }
