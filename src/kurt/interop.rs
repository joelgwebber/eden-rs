    // let frame: HashMap<String, Expr> = hash_map_from!(
    //     "a": expect,
    //     "b": expr,
    // );
    // let b = native_eq(Expr::Dict(ERef::new(frame)));
    // match b {
    //     Expr::Bool(false) => assert!(false),
    //     Expr::Bool(true) => (),
    //     _ => panic!("invalid argument to assert: {}", b)
    // }
