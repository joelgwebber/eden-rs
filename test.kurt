-[ This is a test ]-
-( Of the many ways )-
-{ Of commenting blocks }-
-- As is this

-({
    exprs: [
        ({msg: "foo"} log)
        ({msg: "bar"} log)
        42
    ]
} do)-

-- ({x: 42} x)
-- ({msg: "w00t!"} log)
-- (log "w00t!")

-({x: "w00t"} (x | log x))-

(do [
    (|log "foo")
    (|log "bar")
    (|log "baz")
])

-(do [
    (def {x: "foo"})
    (log x)
])-

-(def {expr: [+ 1 2]})
(expr)

(def {expr: [+ x 2]})
({x: 42} expr)

(def {:fn [x | + x 2]})
(fn 42)
([# [:x] [+ x 2]] 42)
({x: 42} [+ x 2])

(if (> x 1)
    (|log "foo")
    (|log "bar")
)-
